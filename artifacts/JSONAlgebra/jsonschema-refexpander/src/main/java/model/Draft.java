package model;

/**
 * Offers two types of Drafts. It is differed between Draft4 (uses "id") and DraftHigher (uses
 * "$id")
 * 
 * @author Lukas Ellinger
 */
public enum Draft {
  Draft4, DraftHigher
}

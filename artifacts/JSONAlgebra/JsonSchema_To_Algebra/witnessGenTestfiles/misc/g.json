{
  "type": "object",
  "if": {
    "required": [
      "when"
    ],
    "properties": {
      "when": {
        "enum": [
          "delayed"
        ]
      }
    }
  },
  "then": {
    "properties": {
      "when": {
        "enum": [
          "delayed"
        ]
      }
    },
    "required": [
      "when",
      "start_in"
    ]
  }
}

{
    "description": "confirm setup took place",
    "endpoint": "/confirm",
    "methods": [
        "get"
    ],
    "properties": {
        "setup1_token": {
            "enum": [
                "setup1"
            ],
            "type": "string"
        },
        "setup2_token": {
            "enum": [
                "setup2"
            ],
            "type": "string"
        }
    },
    "query": {
        "setup1_token": "${setup1_token}",
        "setup2_token": "${setup2_token}"
    },
    "required": [
        "setup1_token",
        "setup2_token"
    ],
    "type": "object"
}